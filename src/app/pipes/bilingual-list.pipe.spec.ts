import { BilingualListPipe } from './bilingual-list.pipe';

describe('BilingualListPipe', () => {
  it('create an instance', () => {
    const pipe = new BilingualListPipe();
    expect(pipe).toBeTruthy();
  });
});
